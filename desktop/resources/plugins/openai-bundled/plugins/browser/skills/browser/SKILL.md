# Browser

Use when Codex needs browser automation or browser context on Linux.

On this Linux port, Chrome extension connectivity is provided by the local native messaging host registered as `com.openai.codexextension`.
